services:
  - type: web
    name: u7buy-webhook
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_WEBHOOK
        sync: false
      - key: DISCORD_MENTION_USER_ID
        sync: false
      - key: U7BUY_ORDER_URL
        sync: false
